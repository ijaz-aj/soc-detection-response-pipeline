# Copy this file to .env (which is gitignored) and set your own strong value.
# OpenSearch 3.x REQUIRES a strong admin password:
#   min 8 chars, with uppercase, lowercase, number, AND a special character.
OPENSEARCH_INITIAL_ADMIN_PASSWORD=ChangeMe_Str0ng!Pass
